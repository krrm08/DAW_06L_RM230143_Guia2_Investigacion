// Creamos un array
let numeros = [1, 2, 3, 4, 5];

console.log("Array original:", numeros);

// Usamos el método reverse() para invertir el orden de los elementos
numeros.reverse();

console.log("Array después de reverse:", numeros);